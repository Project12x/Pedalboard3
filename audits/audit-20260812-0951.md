# Peer Audit — 2026-08-12 09:51

## Context

- Prior agent: unknown; audit covers the uncommitted work left in the
  `codex/rt-hosting-sprint` checkout after the focused JUCE 9 commit.
- Task claimed: assess whether the historical tuner UI and product-planning
  changes should be folded into current work.
- Files examined: `P0_GIG_SPEED_FEATURES.md`, `ROADMAP.md`,
  `src/TunerControl.cpp`, `src/TunerProcessor.h`,
  `tests/ui_regression_harness_test.cpp`, `tests/tuner_processor_test.cpp`,
  `src/TunerControl.h`, and the JUCE 9 source tree.
- Commands run: `git worktree list`, `git log`, `git diff --check`, focused
  source/implementation searches, and focused Catch2 tuner tests.

## Findings

### Critical

None.

### Important

1. **The tuner change is a real, deliberate presentation update—not inert
   cleanup.** `src/TunerControl.cpp:168-204` grows the node from 390x320 to
   430x350, drops the secondary pitch trace, status badge, and
   reference/response rail, and reinvests the space in a larger note display,
   coarser-deviation strip, and primary meter. This is coherent for a quick
   tuning surface. The retained draw methods at `src/TunerControl.h:54-57` and
   `src/TunerControl.cpp:578`, `:637`, and `:707` are dormant rather than
   broken; delete them only if the product decision is final.

2. **The visual regression test does not validate the rendered result.**
   The focused test at `tests/ui_regression_harness_test.cpp:1985` passed with
   132 assertions, but it checks source-string presence/absence (for example,
   absent call sites at lines 2056, 2057, and 2069). It cannot detect clipped
   controls, poor spacing, or whether removing the live feedback harms tuning
   usability. Capture a screenshot/manual QA result before accepting the UI
   change.

3. **A separate tuner processor test remains failing.**
   `tests/tuner_processor_test.cpp:274` expects the high-E checklist bit to
   remain set after tuning low-E, but the focused run observed it cleared. The
   candidate diff only changes the tuner component size in
   `src/TunerProcessor.h:40`, so it did not cause this failure. Nonetheless,
   the tuner feature is not a green test bundle and should not be folded with
   an assertion of complete validation.

4. **The planning documents are old-dated but substantively current.** Both
   docs declare themselves last updated on 2026-06-20, but the proposed
   `StarterRig`, scratch-template, `StageAction`, and embedded-editor helpers
   still have no implementation in `src/`. Their direction remains coherent:
   scratch templates reuse starter rigs, and a shared Stage action map avoids
   one-off controller behavior. The local JUCE 9 tree contains no CLAP host
   implementation under `modules/juce_audio_processors` or `extras`, so the
   roadmap's conditional CLAP statement remains unmet rather than obsolete.

### Minor

1. **`src/FontManager.h` is not a content change.** Git marks it modified,
   but its index and working-tree blob hashes are both
   `6f725bb367eac9f74b4bf288900f0cddeefd4fa2`, and `git diff --quiet` exits
   successfully. This is a stale stat/line-ending worktree marker, not work to
   fold.

2. **The candidate work is historical, not part of the macOS port.** The
   planning files were last written on 2026-06-20; the tuner files and test
   were last written on 2026-06-22 around 20:04, shortly after commit
   `4b7aa40` at 19:39. The registered `codex/macos-port` worktree points to a
   separate commit and contains no later commit for these paths.

### Discrepancies between summary and code

No prior implementation summary was provided; the scope was inferred from the
remaining uncommitted diff.

The tuner source-contract test passes, and the diff is a genuine visual update,
but the test should not be read as visual proof. The processor checklist test
fails independently of this layout-only diff.

## What was done well

- The candidate diff is clean under `git diff --check` and narrowly scoped.
- The tuner layout update consistently changes both the component size and the
  processor-advertised node size.
- The planning docs record the Element source study, commit, GPL license, and
  pattern-only/clean-room reuse mode, which is appropriate provenance.
- Scratch-template and Stage-action plans have sensible boundaries and avoid
  duplicating existing starter-rig and MIDI-routing systems.

## Recommended next actions

1. Fold the tuner update as its own focused UI commit if the simplified,
   primary-meter-first layout remains the desired product direction. Add a
   changelog entry and a screenshot/manual visual-QA result; resolve the
   separate checklist-test failure in its own fix. Keep or delete the dormant
   drawing methods in a later cleanup, rather than treating that choice as a
   merge blocker.
2. Fold the roadmap/P0 material as a **separate documentation commit**. Refresh
   the June date and add the JUCE 9 context, but retain its current priorities:
   the planned helpers are still absent and the CLAP condition is still unmet.
   It is independent of the macOS port.
3. Leave `src/FontManager.h` out of all commits; it has no content delta. A
   later index refresh may clear its status marker without changing source.
4. Keep the macOS port isolated. If that branch needs the framework update,
   integrate the focused JUCE 9 commit `1e0b4c3` deliberately; do not bring in
   this tuner/roadmap candidate by association.

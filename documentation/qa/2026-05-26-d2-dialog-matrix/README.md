# D2 Dialog Visual QA Pass - 2026-05-26

Build under test: `build/Pedalboard3_artefacts/Release/Pedalboard3.exe`

Source baseline before this pass: `525d5b7`

Capture environment:

- Windows monitor DPI reported by `GetDpiForWindow`: 168
- Effective scale: 175%
- QA patch: generated by `scripts/run_d2_visual_qa.ps1` and temporarily installed as `%APPDATA%/Pedalboard3/default.pdl`
- User settings and default patch were backed up before capture and restored after capture.

Automation changes covered:

- Added deterministic visual-QA command-line hooks for Plugin Search, Preferences, NAM Model Browser, and IR Browser.
- Extended `scripts/run_d2_visual_qa.ps1` to enumerate top-level app windows and capture dialog handles directly.
- Preserved existing main-shell, dense-graph, Stage Mode, and patch-switch captures in the same run.

Evidence captured:

- Main shell and workflow captures:
  - `theme-midnight-main.png`
  - `theme-daylight-main.png`
  - `theme-synthwave-main.png`
  - `theme-deep-ocean-main.png`
  - `theme-forest-main.png`
  - `workflow-dense-graph.png`
  - `workflow-stage-mode-before-switch.png`
  - `workflow-stage-mode-after-patch-next.png`
  - `workflow-main-after-patch-next.png`
- Dialog captures for every built-in theme:
  - `dialog-plugin-search-*.png`
  - `dialog-preferences-*.png`
  - `dialog-nam-browser-*.png`
  - `dialog-ir-browser-*.png`
- `capture-summary.json`

Observed notes:

- Plugin Search, Preferences, NAM Model Browser, and IR Browser all opened deterministically from command-line QA flags.
- Dialog captures exist for Midnight, Daylight, Synthwave, Deep Ocean, and Forest.
- Preferences had a fixed-coordinate layout issue where the fourth Visible I/O Nodes row crowded the Midi Options heading; this pass fixes the panel height and shifts following groups down.
- Daylight dialog contrast is readable after the footer contrast pass.

Remaining matrix work:

- Exact 100%, 150%, and 200% DPI captures are still pending because this workstation was at 175% during the automated run.
